# Exemplos do Aedis

Quickstarts públicos e neutros (API + Worker) serão adicionados aqui, demonstrando o uso dos pacotes `Aedis.*` e o *package swap* (ex.: trocar RabbitMQ por AWS SQS sem tocar no domínio).

> O consumidor de referência (dogfooding) é privado; estes exemplos são sintéticos.
